const expect = require('expect');
const request = require('supertest');
const app = require('./app').app;


it ('should return ')
it("testing", () => {
	var res = "hello";
	
	expect(res).toExist; 
});